"""add prediction in_tracked_universe field

Revision ID: 6b0f3a85b65d
Revises: dad98268679f
Create Date: 2026-07-20 16:50:52.599847
"""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa


revision = '6b0f3a85b65d'
down_revision = 'dad98268679f'
branch_labels = None
depends_on = None


def upgrade() -> None:
    # server_default=true: every prediction written before this migration
    # was created back when the tracked universe was a hard constraint on
    # what the model could name, so backfilling existing rows as "in
    # universe" is factually correct, not a guess.
    op.add_column(
        'prediction',
        sa.Column('in_tracked_universe', sa.Boolean(), nullable=False, server_default=sa.true()),
    )
    op.create_index(op.f('ix_prediction_in_tracked_universe'), 'prediction', ['in_tracked_universe'], unique=False)


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_index(op.f('ix_prediction_in_tracked_universe'), table_name='prediction')
    op.drop_column('prediction', 'in_tracked_universe')
    # ### end Alembic commands ###
